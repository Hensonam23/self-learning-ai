# MachineSpirit (Local AI + Web App)

MachineSpirit is a local learning AI that runs from `brain.py` with a small local API (`ms_api.py`) and a simple UI (`ms_ui.py`).

## Quick Start (one command)

Clone this repo into your home folder as `~/self-learning-ai`, then run:

```bash
cd ~/self-learning-ai
./scripts/install.sh
